package com.univ.daoimpl;

import java.util.List;

import org.springframework.jdbc.core.BeanPropertyRowMapper;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.datasource.DriverManagerDataSource;

import com.univ.dao.EmployeeDao;
import com.univ.pojo.Employee;

public class EmployeeDoaImpl implements EmployeeDao {
	 
private JdbcTemplate jTemplate;
	//setter for jdbctemplateuu
	public void setjTemplate(JdbcTemplate jTemplate) {
		this.jTemplate = jTemplate;
	}
	
	

	@Override
	public boolean addEmployee(Employee e) {
		try {
			int count=jTemplate.update("insert into Employee values(?,?,?))",
					new Object[] {e.getEmpId(),e.getEmpName(),e.getSalary()});
		   
			if(count>0)
			{
				return true;
			}
			else
			{
				return false;
			}
			
		} catch (Exception e2) {
			// TODO: handle exception
			e2.printStackTrace();
			return false;
		}
	
	}

	@Override
	public boolean updateEmployee(Employee e) {
		// TODO Auto-generated method stub
		try {
			int count=jTemplate.update("update employee set empName=?"
					+ ",salary=?)",
					new Object[] {e.getEmpId(),e.getEmpName(),e.getSalary()});
		   
			if(count>0)
			{
				return true;
			}
			else
			{
				return false;
			}
			
		} catch (Exception e2) {
			// TODO: handle exception
			e2.printStackTrace();
			return false;
		}
	
	}

	@Override
	public boolean deleteEmployee(Employee e) {
		// TODO Auto-generated method stubtry {
		int count=jTemplate.update("delete from employee where empId=?)",
				});
	   
		if(count>0)
		{
			return true;
		}
		else
		{
			return false;
		}
		
	} catch (Exception e2) {
		// TODO: handle exception
		e2.printStackTrace();
		return false;
	}

		
	}

	@Override
	public boolean searchEmployee(int empId) {
		// TODO Auto-generated method stub
		try {
			List<Employee> lst=jTemplate.query("select * from employee where empId=?", new BeanPropertyRowMapper<Employee>(Employee.class),empId);
	       if(lst.size()>0)
	       {
	    	return lst.get(0);   
	       }
	       else
	       {
	    	   return null;
	       }
		
		} catch (Exception e) {
			e2.printStackTrace();
			return false;
		}
		
	}

	@Override
	public List<Employee> getAllEmployee() {
		// TODO Auto-generated method stub
		return null;
	}

	
}
